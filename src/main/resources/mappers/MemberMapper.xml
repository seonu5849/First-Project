<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.myapp.repository.MemberMabatisXmlDAO">
    <select id="selectAll" resultType="org.zerock.myapp.domain.MemberVO">
        select
            *
        from
            member
    </select>

    <insert id="insertMember">
        insert into member(member_no, member_id, member_pw, member_name, member_phone)
        values(seq_member.NEXTVAL, #{memberId}, #{memberPw}, #{memberName}, #{memberPhone})
    </insert>

    <update id="updateMember">
        update member set member_phone = #{memberPhone}
        where member_no = #{memberNo}
    </update>

    <delete id="deleteMember">
        delete from member
        where member_no = #{memberNo}
    </delete>

    <select id="findById" resultType="string">
        select
            member_id
        from member
        where
            member_name = #{memberName}
            AND member_phone = #{memberPhone}
    </select>
</mapper>